name: api
description: Dart package which manages the api.
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.1
  protobuf: ^1.0.1
  exceptions:
    path: '../exceptions'
  connection:
    path: '../connection'
  discovery:
    path: '../discovery'
  pdelta:
    path: "../../../protod/packages/pdelta"
  pmsg:
    path: "../../../protod/packages/pmsg"

dev_dependencies:
  flutter_test:
    sdk: flutter
