name: data
description: Dart package which manages data.
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auth:
    path: "../auth"
  api:
    path: "../api"
  exceptions:
    path: "../exceptions"
  pdelta:
    path: "../../../protod/packages/pdelta"
  pserver:
    path: "../../../protod/packages/pserver"

dev_dependencies:
  flutter_test:
    sdk: flutter
